# Nom de l'application
spring.application.name=Quize

# Configuration de la base de données
spring.datasource.url=jdbc:mysql://serverquiz1.mysql.database.azure.com:3306/quiz_platform?useSSL=true
spring.datasource.username=quizroot
spring.datasource.password=AREajd405

# Driver MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Afficher les requêtes SQL dans la console (optionnel)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
